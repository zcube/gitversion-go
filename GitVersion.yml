# 다음 릴리스의 baseline. 한 사이클용 floor 이며 영구 고정값이 아니다.
# v0.1.0 이상으로 태그하면 태그가 이 값을 가리므로(같거나 높음), 다음 사이클에는
# 이 값을 상향하거나 더 높은 v* 태그를 만든다(태그가 항상 최종 소스). 자세한 내용은 README.
workflow: GitHubFlow/v1
next-version: 0.1.0
